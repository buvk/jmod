#ifndef JMOD_ITEM_STAT_RANGES_H
#define JMOD_ITEM_STAT_RANGES_H

/* Install the Diablo II 1.09b item-tooltip range hook.
   Returns zero when the expected client/common layout is not present. */
int item_stat_ranges_init(void);

#endif

